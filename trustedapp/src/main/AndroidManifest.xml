<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.dicekeys.trustedapp">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DiceKeys Trusted App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
    >
        <activity android:name="org.dicekeys.trustedapp.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.dicekeys.trustedapp.activities.DisplayPublicKeyActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="org.dicekeys.trustedapp.activities.ExecuteApiCommandActivity"
            android:theme="@style/AppTheme"
            android:exported="true">
            <intent-filter android:label="web-filter">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="dicekeys.org"
                    android:pathPrefix="/api"
                />
            </intent-filter>
        </activity>

    </application>

</manifest>